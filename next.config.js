/** @type {import('next').NextConfig} */
const nextConfig = {
  // We use a custom server (server.ts) so we don't need next's built-in server
  // The app is still compiled by next build
};

module.exports = nextConfig;
